import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv(r"C:\Users\hp victus\Downloads\Boston (1).csv")
df.head(10)

df.drop(columns=['Unnamed: 15','Unnamed: 16'],inplace=True)
df.drop(columns=['CAT. MEDV'],inplace=True)

df.isnull().sum()

df.info()

df.describe()

df.corr()['MEDV'].sort_values()

X = df.loc[:,['LSTAT','PTRATIO','RM']]
Y = df.loc[:,"MEDV"]
X.shape,Y.shape

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(X,Y,test_size=0.25,random_state=10)
from sklearn.preprocessing import StandardScaler
scaler = StandardScaler()

scaler.fit(x_train)

x_train = scaler.transform(x_train)
x_test = scaler.transform(x_test)
from keras.models import Sequential
from keras.layers import Dense

model = Sequential()

model.add(Dense(128,input_shape=(3,),activation='relu',name='input'))
model.add(Dense(64,activation='relu',name='layer_1'))
model.add(Dense(1,activation='linear',name='output'))
model.compile(optimizer='adam', loss='mse', metrics=['mae'])
model.summary()

model.fit(x_train,y_train,epochs=100,validation_split=0.05)

output = model.evaluate(x_test,y_test)

print(f"Mean Squared Error: {output[0]}"
      ,f"Mean Absolute Error: {output[1]}",sep="\n")
y_pred = model.predict(x=x_test)
print(*zip(y_pred,y_test))




















































"""
हा code Deep Learning वापरून Boston Housing Dataset मधील house prices predict करतो.

Dataset load केला
Unwanted columns delete केले
Input features (LSTAT, PTRATIO, RM) आणि output (MEDV) घेतले
Data train-test मध्ये divide केला
StandardScaler ने data normalize केला
Neural Network model तयार केला
Model train करून testing data वर evaluate केला
शेवटी house price prediction केले

1. What is Deep Neural Network (DNN)?
A DNN is a machine learning model made of multiple neural network layers used for learning complex patterns from data.

2. What is the Boston Housing Dataset?
It is a dataset containing information about houses in Boston used for house price prediction.

3. What is regression?
Regression is a machine learning technique used to predict continuous numerical values.

4. Why is MEDV used as target variable?
Because MEDV represents the median value of houses, which we want to predict.

5. What is the purpose of train-test split?
It divides data into training and testing sets to evaluate model performance.

6. Why is StandardScaler used?
It normalizes data so all features are on a similar scale.

7. What is ReLU activation function?
ReLU returns positive values and converts negative values to zero.

8. Why is linear activation used in output layer?
Because regression problems require continuous numerical output.

9. What is Sequential model in Keras?
Sequential model is a linear stack of neural network layers.

10. What is a Dense layer?
A Dense layer is a fully connected neural network layer.

11. What is an epoch?
One complete cycle of training through the entire dataset.

12. What is an optimizer?
Optimizer updates model weights to reduce error during training.

13. What is Adam optimizer?
Adam is an efficient optimization algorithm used in deep learning.
"""